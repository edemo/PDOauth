before_install:
- "export DISPLAY=:99.0"
- "sh -e /etc/init.d/xvfb start"
- sudo apt-get update
- sudo apt-get install python make python-pip firefox
- sudo pip install -r requirements.txt
script:
- make
